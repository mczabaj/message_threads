<%= form_with(model: message_thread, local: true) do |form| %>
  <% if message_thread.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message_thread.errors.count, "error") %> prohibited this message_thread from being saved:</h2>

      <ul>
      <% message_thread.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :message_thread_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
